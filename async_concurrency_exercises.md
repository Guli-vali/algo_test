# 🚀 Упражнения и вопросы: Асинхронность и конкурентность

> Подготовка к собеседованиям в Ozon, Яндекс, Авито, VK

---

## 📝 Теоретические вопросы (часто спрашивают на интервью)

### 1. Основы async/await

**Q1.1:** Что такое event loop? Как он работает?

**Q1.2:** В чем разница между `async def` и обычной функцией?

**Q1.3:** Что произойдет, если вызвать `async` функцию без `await`?

**Q1.4:** Объясни разницу между:
```python
await asyncio.gather(task1(), task2(), task3())
```
и
```python
await task1()
await task2()
await task3()
```

**Q1.5:** Что такое корутина (coroutine)? Чем она отличается от генератора?

---

### 2. Blocking vs Non-blocking

**Q2.1:** Почему `time.sleep(1)` "ломает" async-код? Что использовать вместо?

**Q2.2:** Какие операции в Python являются blocking? Приведи примеры.

**Q2.3:** Как правильно делать HTTP-запросы в async-коде?

**Q2.4:** Что произойдет, если в async-функции вызвать синхронный блокирующий код?

---

### 3. Async vs Threading vs Multiprocessing

**Q3.1:** Когда использовать `asyncio`, когда `threading`, когда `multiprocessing`?

**Q3.2:** Может ли async-код работать быстрее, чем threading для I/O-операций? Почему?

**Q3.3:** Когда async **не нужен**? Приведи примеры.

**Q3.4:** Можно ли использовать `threading` внутри async-кода? Когда это имеет смысл?

**Q3.5:** Что такое GIL? Как он влияет на threading и multiprocessing?

---

### 4. Практические паттерны

**Q4.1:** Как ограничить количество одновременных запросов? (rate limiting в async)

**Q4.2:** Как обработать ошибки в `asyncio.gather`?

**Q4.3:** Что такое `asyncio.create_task()`? Когда его использовать?

**Q4.4:** Как правильно закрывать ресурсы в async-коде? (context managers)

**Q4.5:** Что такое `asyncio.Semaphore`? Для чего он нужен?

---

## 💻 Практические упражнения

### Упражнение 1: Базовый async/await

**Задача:** Напиши функцию, которая делает 3 HTTP-запроса параллельно и возвращает результаты.

**Подсказка:** Используй `aiohttp` или `httpx`.

**Ожидаемый результат:**
```python
async def fetch_urls(urls):
    # твой код здесь
    pass

# Использование:
results = await fetch_urls([
    'https://api.example.com/data1',
    'https://api.example.com/data2',
    'https://api.example.com/data3'
])
```

---

### Упражнение 2: Исправь блокирующий код

**Задача:** Этот код блокирует event loop. Исправь его:

```python
import asyncio
import time

async def process_items(items):
    results = []
    for item in items:
        # Эта операция блокирует event loop
        time.sleep(0.1)  # Имитация тяжелой операции
        results.append(item * 2)
    return results

async def main():
    items = list(range(10))
    results = await process_items(items)
    print(results)

asyncio.run(main())
```

**Вопросы для размышления:**
- Что не так с этим кодом?
- Как правильно исправить?
- Что если операция действительно CPU-bound?

---

### Упражнение 3: Rate limiting с Semaphore

**Задача:** Напиши функцию, которая делает запросы к API, но не более 3 одновременно.

```python
import asyncio

async def fetch_with_limit(url, semaphore):
    # твой код здесь
    pass

async def main():
    urls = [f'https://api.example.com/data{i}' for i in range(10)]
    semaphore = asyncio.Semaphore(3)
    
    tasks = [fetch_with_limit(url, semaphore) for url in urls]
    results = await asyncio.gather(*tasks)
    return results
```

---

### Упражнение 4: Обработка ошибок в gather

**Задача:** Напиши код, который делает несколько запросов, но продолжает работу даже если некоторые падают с ошибкой.

**Требования:**
- Если запрос упал, вернуть `None` вместо результата
- Все успешные запросы должны быть обработаны
- Использовать `asyncio.gather`

---

### Упражнение 5: Timeout для async операций

**Задача:** Напиши функцию, которая делает запрос с таймаутом 5 секунд. Если запрос не успел, верни `None`.

```python
async def fetch_with_timeout(url, timeout=5):
    # твой код здесь
    pass
```

**Подсказка:** Используй `asyncio.wait_for()`.

---

### Упражнение 6: Async context manager

**Задача:** Создай async context manager для подключения к базе данных.

```python
class AsyncDBConnection:
    async def __aenter__(self):
        # инициализация подключения
        pass
    
    async def __aexit__(self, exc_type, exc_val, exc_tb):
        # закрытие подключения
        pass

# Использование:
async def main():
    async with AsyncDBConnection() as db:
        # работа с БД
        pass
```

---

### Упражнение 7: Сравнение async vs threading

**Задача:** Напиши два варианта функции, которая делает 10 HTTP-запросов:
1. С использованием `asyncio`
2. С использованием `threading.Thread`

Измерь время выполнения обоих вариантов и объясни разницу.

---

### Упражнение 8: Producer-Consumer с asyncio.Queue

**Задача:** Реализуй паттерн producer-consumer с использованием `asyncio.Queue`.

```python
import asyncio

async def producer(queue, items):
    # добавляет items в очередь
    pass

async def consumer(queue):
    # обрабатывает items из очереди
    pass

async def main():
    queue = asyncio.Queue(maxsize=10)
    items = list(range(20))
    
    # Запусти producer и несколько consumers
    # ...
```

---

## 🎯 Типовые задачи с собеседований

### Задача 1: Параллельная обработка файлов

**Условие:** Есть список путей к файлам. Нужно прочитать все файлы параллельно и вернуть их содержимое.

**Требования:**
- Максимум 5 файлов одновременно
- Обработать ошибки (если файл не найден)
- Использовать async

---

### Задача 2: Кэширование с TTL

**Условие:** Реализуй async-функцию с кэшированием результатов на 60 секунд.

```python
@async_cache(ttl=60)
async def expensive_operation(key):
    # тяжелая операция
    pass
```

---

### Задача 3: Graceful shutdown

**Условие:** Напиши сервер, который:
- Принимает запросы
- При получении сигнала SIGTERM завершает текущие запросы
- Не принимает новые запросы
- Корректно закрывает все соединения

---

### Задача 4: Retry с exponential backoff

**Условие:** Напиши декоратор для повторных попыток с экспоненциальной задержкой.

```python
@retry(max_attempts=3, base_delay=1)
async def unreliable_api_call():
    # может упасть с ошибкой
    pass
```

---

## 🔍 Вопросы на понимание (часто задают)

### Вопрос 1: Что выведет этот код?

```python
import asyncio

async def task(name, delay):
    print(f"{name} started")
    await asyncio.sleep(delay)
    print(f"{name} finished")
    return name

async def main():
    print("Main started")
    result = await asyncio.gather(
        task("A", 2),
        task("B", 1),
        task("C", 3)
    )
    print(f"Results: {result}")
    print("Main finished")

asyncio.run(main())
```

**Вопрос:** В каком порядке выведутся сообщения? Почему?

---

### Вопрос 2: Где ошибка?

```python
import asyncio
import requests

async def fetch_data(url):
    response = requests.get(url)
    return response.json()

async def main():
    urls = ['https://api.example.com/data1', 'https://api.example.com/data2']
    results = await asyncio.gather(*[fetch_data(url) for url in urls])
    return results
```

**Вопрос:** Что не так с этим кодом? Как исправить?

---

### Вопрос 3: Производительность

**Вопрос:** Будет ли этот код работать быстрее с `asyncio`?

```python
def process_data(data):
    # CPU-intensive операция
    result = sum(x**2 for x in range(1000000))
    return result

data_list = [list(range(100)) for _ in range(10)]
```

**Ответ:** Нет, потому что...

---

## 📚 Дополнительные темы для изучения

1. **asyncio.Lock** — когда нужен в async-коде?
2. **asyncio.Event** — для чего используется?
3. **asyncio.Queue** — отличия от обычной queue.Queue
4. **asyncio.run()** vs `loop.run_until_complete()` — в чем разница?
5. **Task vs Future** — что это и когда использовать?
6. **Cancellation** — как правильно отменять задачи?

---

## ✅ Чек-лист готовности

Перед собеседованием убедись, что ты можешь:

- [ ] Объяснить, что такое event loop
- [ ] Написать async-функцию с несколькими await
- [ ] Использовать `asyncio.gather` для параллельных операций
- [ ] Объяснить, почему `time.sleep()` блокирует async
- [ ] Использовать `asyncio.Semaphore` для rate limiting
- [ ] Обработать ошибки в async-коде
- [ ] Объяснить разницу между async, threading и multiprocessing
- [ ] Назвать случаи, когда async не нужен
- [ ] Объяснить GIL на базовом уровне
- [ ] Написать async context manager

---

## 🎓 Ресурсы для практики

1. **Официальная документация:** https://docs.python.org/3/library/asyncio.html
2. **Real Python:** Async IO в Python
3. **Практика:** Напиши простой async HTTP-сервер
4. **Практика:** Реализуй async-клиент для работы с API

---

**Удачи на собеседовании! 🚀**

